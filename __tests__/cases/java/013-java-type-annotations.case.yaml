args:
  - input/TypeAnnotations.java
input/TypeAnnotations.java: |
  package com.example;

  import org.jetbrains.annotations.*;

  public class TypeAnnotations {
      // Annotation on type parameter
      List<@NonNull String> names;

      // Annotation on array type
      String @NonNull [] annotatedArray;

      // Annotation on type bound
      <T extends @NonNull Object> T boundedMethod(T value) {
          return value;
      }

      // Multiple annotations on type
      Map<@NonNull String, @Nullable Integer> map;

      // Annotation on wildcard
      List<? extends @NonNull Number> list;

      // Annotation in method signature
      void process(@NonNull String input) {
      }

      // Annotation on return type
      @NonNull String getValue() {
          return "value";
      }
  }
stdout: |
  input/TypeAnnotations.java
  [L5-L31] public class TypeAnnotations
    [L7-L7] List<@NonNull String> names
    [L10-L10] String @NonNull [] annotatedArray
    [L13-L15] <T extends @NonNull Object> T boundedMethod(T value)
    [L18-L18] Map<@NonNull String, @Nullable Integer> map
    [L21-L21] List<? extends @NonNull Number> list
    [L24-L25] void process(@NonNull String input)
    [L28-L30] @NonNull String getValue()

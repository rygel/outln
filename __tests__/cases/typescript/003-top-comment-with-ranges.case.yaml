args:
  - input/commented.ts
input/commented.ts: |
  // CLI entrypoint helpers.
  export function parse(input: string): string {
    return input.trim();
  }
stdout: |
  input/commented.ts
  // CLI entrypoint helpers.
  [L2-L4] export function parse(input: string): string

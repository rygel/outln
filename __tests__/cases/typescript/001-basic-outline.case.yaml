args:
  - input/basic.ts
input/basic.ts: |
  interface Person {
    name: string;
  }

  type Status = 'on' | 'off';

  class Counter {}

  function formatName(name: string): string {
    return name;
  }

  const ready = true;
stdout: |
  input/basic.ts
  [L1-L3] interface Person
  [L5-L5] type Status
  [L7-L7] class Counter
  [L9-L11] function formatName(name: string): string
  [L13-L13] const ready

args:
  - input/mixed.ts
input/mixed.ts: |
  export interface Config {
    mode: string;
  }

  export enum Kind {
    One = 1,
    Two = 2
  }

  class PlainClass {
    value = 1;
  }
stdout: |
  input/mixed.ts
  [L1-L3] export interface Config
  [L5-L8] export enum Kind
  [L10-L12] class PlainClass

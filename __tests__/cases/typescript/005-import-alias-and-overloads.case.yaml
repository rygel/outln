args:
  - input/import-alias-and-overloads.ts
input/import-alias-and-overloads.ts: |
  import fs = require('node:fs');
  import Join = fs.readFileSync;
  function read(path: string): string;
  function read(fd: number): Buffer;
  function read(pathOrFd: string | number): string | Buffer {
    return typeof pathOrFd === 'string' ? '' : Buffer.alloc(0);
  }
stdout: |
  input/import-alias-and-overloads.ts
  [L1-L1] import fs
  [L2-L2] import Join
  [L3-L3] function read(path: string): string
  [L4-L4] function read(fd: number): Buffer
  [L5-L7] function read(pathOrFd: string | number): string | Buffer

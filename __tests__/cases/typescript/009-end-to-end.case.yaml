args:
  - input/src
  - input/docs/
  - input/config/*.ts
input/src/utils.ts: |
  /** Utility functions. */
  export const utils = {};
input/src/helpers/nested.ts: |
  /** Helper functions. */
  export const helpers = {};
input/docs/readme.md: |
  ---
  title: Documentation
  ---
input/docs/guide.md: |
  # User Guide
input/config/settings.ts: |
  /** Config settings. */
  export const settings = {};
stdout: |
  glob view: input/src/**/* input/docs/**/* input/config/*.ts
  Includes only header comments per file.
  For file-level outlines, use `outln [FILE]...`.
  input/config/settings.ts: Config settings.
  input/docs/guide.md: (no header comment available)
  input/docs/readme.md: title: "Documentation"
  input/src/helpers/nested.ts: Helper functions.
  input/src/utils.ts: Utility functions.

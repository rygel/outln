args:
  - input/**/*
input/alpha.js: |
  // Alpha summary
  const alpha = 1;
input/bravo.jsx: |
  /** Bravo summary */
  export function Bravo() {
    return <div />;
  }
input/charlie.mjs: |
  export const charlie = 3;
input/delta.txt: |
  plain text
stdout: |
  glob view: input/**/*
  Includes only header comments per file.
  For file-level outlines, use `outln [FILE]...`.
  input/alpha.js: Alpha summary
  input/bravo.jsx: Bravo summary
  input/charlie.mjs: (no header comment available)
stderr: FILE input/delta.txt HAS UNSUPPORTED FILE TYPE

args:
  - input/complex.rs
input/complex.rs: |
  // Complex functions

  const fn compute(x: u32) -> u64 { x as u64 }
  unsafe fn dangerous() {}
  async fn fetch() -> Result<(), Error> { Ok(()) }
  extern "C" fn callback(data: *mut c_void) {}
  pub fn public_func() {}
  unsafe async fn unsafe_async() {}
  fn generic<T: Display>(value: T) where T: Clone {}
stdout: |
  input/complex.rs
  // Complex functions
  [L3-L3] const fn compute (x: u32) -> u64
  [L4-L4] unsafe fn dangerous ()
  [L5-L5] async fn fetch () -> Result<(), Error>
  [L6-L6] extern "C" fn callback (data: *mut c_void)
  [L7-L7] pub fn public_func ()
  [L8-L8] unsafe async fn unsafe_async ()
  [L9-L9] fn generic <T: Display> (value: T) where T: Clone

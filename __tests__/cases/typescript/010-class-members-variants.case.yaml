args:
  - input/member-variants.ts
input/member-variants.ts: |
  abstract class Example {
    private value = 0;
    static async load(id: string): Promise<Example> { throw new Error("x"); }
    #touch(): void {}
    [Symbol.iterator](): Iterator<number> { return [][Symbol.iterator](); }
    abstract run(task: string): number;
  }
stdout: |
  input/member-variants.ts
  [L1-L7] abstract class Example
    [L3-L3] load(id: string): Promise<Example>
    [L4-L4] #touch(): void
    [L6-L6] run(task: string): number
